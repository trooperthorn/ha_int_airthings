pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-homeassistant-custom-component
syrupy>=6.0.0
mypy>=1.10.0
ruff>=0.16.5
# pytest-homeassistant-custom-component's release metadata can lag the final
# Home Assistant release; pin explicitly so the harness re-resolves to
# stable instead of the beta it was built from.
homeassistant==2026.9.0
# Transitive imports of homeassistant.components.usb (pulled in by
# homeassistant.components.bluetooth) that the homeassistant package itself
# does not declare as install requirements.
serialx==1.9.0
aiousbwatcher==1.1.2
